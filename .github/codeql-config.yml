name: "armada.nu CodeQL config"

# Keep all default security queries
disable-default-queries: false

# Exclude paths that are generated, vendored, or not application code.
paths-ignore:
  - node_modules
  - .next
  - public
  - "**/*.min.js"
  - "src/scripts"

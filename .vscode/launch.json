{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Frontend (Prod CMS)",
      "type": "editor-browser",
      "request": "launch",
      "url": "http://localhost:8000",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "dev (Prod CMS)"
    },
    {
      "name": "Frontend (Local CMS)",
      "type": "editor-browser",
      "request": "launch",
      "url": "http://localhost:8000",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "dev (Local CMS)"
    }
  ]
}
